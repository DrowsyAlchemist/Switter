{
  "ConnectionStrings": {
    "PostgreSQL": "Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
  },
  "Redis": {
    "ConnectionString": "${REDIS_HOST}:${REDIS_PORT},password=${REDIS_PASSWORD},abortConnect=false"
  },
  "Kafka": {
    "BootstrapServers": "Kafka:BootstrapServers=${KAFKA_HOST}:${KAFKA_PORT}"
  },
  "Jwt": {
    "Secret": "development-secret-key",
    "Issuer": "switter-dev",
    "Audience": "switter-dev"
  }
}
